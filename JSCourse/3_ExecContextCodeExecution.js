function b(){
    console.log('Called b');
}

b();


console.log(a);
var a = 'Hello World';
console.log(a);

// Single thread sync execution
// Single threaded, one command at a time
// Synchronous, one at a time, one line of code at a time, in order that it appears.
// in JS it happes one a time