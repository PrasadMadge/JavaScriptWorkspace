

/*
JS let variable

usage of let variables before its intialized gives error.

*/

// imp, let var declared inside {} are only available inside it.

var a=1;
var b = 2;
if(a<b){
    let c = 3;
}

// also applicable to loop statements, will get a diff variable each time the loop is running
